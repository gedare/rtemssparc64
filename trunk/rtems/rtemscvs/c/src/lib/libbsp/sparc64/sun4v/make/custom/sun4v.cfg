#
#  Config file for the Niagara SPARC64 processor.
#
#  $Id$
#
include $(RTEMS_ROOT)/make/custom/default.cfg

RTEMS_CPU=sparc64
RTEMS_CPU_MODEL=sun4v

# This is the actual bsp directory used during the build process.
RTEMS_BSP_FAMILY=sun4v

#  This contains the compiler options necessary to select the CPU model
#  and (hopefully) optimize for it.
#CPU_CFLAGS = -mcpu=niagara -msoft-float
#CPU_CFLAGS = -mcpu=niagara -DRTEMS_DEBUG -DDEBUG_WORKSPACE
#CPU_CFLAGS = -mcpu=niagara -DRTEMS_DEBUG -DDEBUG_WORKSPACE -DHAVE___ASSERT_FUNC -DBSP_GET_WORK_AREA_DEBUG -DGICADEBUG
CPU_CFLAGS = -mcpu=niagara
#CPU_CFLAGS = -mcpu=niagara -DRTEMS_DEBUG -DHAVE___ASSERT_FUNC 

# optimize flag: typically -O2
CFLAGS_OPTIMIZE_V = -gstabs
