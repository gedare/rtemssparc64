Naming convention
  cbench_SP_xt_yf:
    SP is scheduling policy, one of:
      ts=timeslice
      rm=rate monotonic (periodic)
      co=cooperative (non-preemptive)
    
    xxxt is xxx number (0 fill) of tasks

    yyyyf is yyyy (0 fill) number of functions

